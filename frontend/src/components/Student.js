export default function Student({name,address,id}){

    return (

        <div className="containerStudent">
            <div>Name: {name}</div>
            <div>Address: {address}</div>
        </div>
    )
}